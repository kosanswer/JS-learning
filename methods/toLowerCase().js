/*
В JavaScript метод `toLowerCase()` применяется к строке и возвращает новую строку, в которой все символы преобразованы в нижний регистр.

Синтаксис метода `toLowerCase()` выглядит следующим образом:

```javascript
str.toLowerCase()
```

- `str`: Исходная строка, которую нужно преобразовать в нижний регистр.

Вот пример использования метода `toLowerCase()`:

```javascript
const str = "Hello World";
const lowerCaseStr = str.toLowerCase();
console.log(lowerCaseStr);  // Вывод: "hello world"
```

В этом примере переменная `str` содержит строку `"Hello World"`. 
При применении метода `toLowerCase()` к этой строке создается новая строка `"hello world"`, в которой все символы преобразованы в нижний регистр.

Метод `toLowerCase()` не изменяет исходную строку, а возвращает новую строку с преобразованными символами.
*/
